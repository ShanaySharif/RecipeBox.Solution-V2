<!-- @{
  Layout = "_Layout";
}

@using RecipeBox.Models;

<h1>Welcome to RecipeBox!</h1>

<hr>

<h4>recipes</h4>
@if (Model["recipes"].Length == 0)
{
  <p>No recipes have been added yet!</p>
} 
<ul>
  @foreach (Recipe cat in Model["Recipe"])
  {
    <li>@Html.ActionLink(@cat.Name, "Details", "Recipes", new { id = @cat.CategoryId})</li>
  }
</ul>

<h4>Recipes</h4>
@if (Model["recipe"].Length == 0)
{
  <p>No recipes have been added yet!</p>
} 
<ul>
  @foreach (Recipe recipe in Model["items"])
  {
    <li>@Html.ActionLink(@item.Description, "Details", "Items", new { id = @item.ItemId})</li>
  }
</ul>

<hr>

<p>@Html.ActionLink("Manage items", "Index", "Items")</p> 
<p>@Html.ActionLink("Manage recipes", "Index", "recipes")</p>
<p>@Html.ActionLink("Manage tags", "Index", "Tags")</p> -->

@{
    Layout = "_Layout";
}

@using RecipeBox.Models;

<h1>Welcome to RecipeBox!</h1>

<hr>

<h4>Recipes</h4>
@if (Model.Count == 0)
{
    <p>No recipes have been added yet!</p>
}
<ul>
    @foreach (var recipe in Model)
    {
        <li>@Html.ActionLink(@recipe.Name, "Details", "Recipes", new { id = recipe.RecipeId })</li>
    }
</ul>

<hr>

<p>@Html.ActionLink("Manage items", "Index", "Items")</p>
<p>@Html.ActionLink("Manage recipes", "Index", "Recipes")</p>
<p>@Html.ActionLink("Manage tags", "Index", "Tags")</p>