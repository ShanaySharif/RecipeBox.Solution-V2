@model RecipeBox.Models.Recipe

<div class="container">
    <h1>Edit Recipe</h1>
    <h4>Editing: @Html.DisplayFor(model => model.RecipeName)</h4>
</div>
<hr />

<div class="container">
    @using (Html.BeginForm())
    {
        <div class="form-container">
            @Html.HiddenFor(model => model.RecipeId)
            <div class="form-group">
                <label class="form-label">@Html.DisplayNameFor(model => model.RecipeName)</label>
                @Html.EditorFor(model => model.RecipeName, new { @class = "form-control" })
                <p class="text-danger">@Html.ValidationMessageFor(model => model.RecipeName)</p>
            </div>
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-success">Save</button>
        </div>
    }
</div>
<hr />

<div class="container">
    <div class="link-container">
        <p>@Html.ActionLink("Delete Engineer", "Delete", new { id = Model.RecipeId }, new { @class = "btn btn-danger" })</p>
        <p>@Html.ActionLink("Back to All Engineers", "Index", null, new { @class = "btn btn-secondary" })</p>
    </div>
</div>